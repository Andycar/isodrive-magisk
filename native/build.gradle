plugins {
    id 'com.android.library'
}

android {
    namespace 'com.nitanmarcel.isodrive.native'
    compileSdk 34

    defaultConfig {
        minSdk 21
    }

    externalNativeBuild {
        ndkBuild {
            path("jni/Android.mk")
        }
    }
}
